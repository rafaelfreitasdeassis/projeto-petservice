
const API_URL = 'html://localhost:3000/usuarios';       // Configuração da URL base da API

const formUsuario = document.getElementById('form-usuario');   // Seleção de elementos do DOM

const payload = {
    nome: document
}